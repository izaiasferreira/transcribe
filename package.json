{
  "name": "teste",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "apt-get update && apt-get install -y build-essential python3 python3-pip python3-dev libffi-dev wget unzip && apt-get clean && rm -rf /var/lib/apt/lists/* && wget https://alphacephei.com/vosk/models/vosk-model-small-pt-0.3 && unzip vosk-model-small-pt-0.3 && node app.js",
    "dev": "node app.js",
    "nodemon": "nodemon app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "express": "^4.19.2",
    "ffmpeg": "^0.0.4",
    "fluent-ffmpeg": "^2.1.3",
    "vosk": "^0.3.39"
  }
}
